Edunova🎓

A simple Node.js & Express API to manage student records — add, view, update, and delete students.

Setup 💻
git clone https://github.com/<your-username>/edunova.git
cd edunova
npm install
node index.js

Server runs at http://localhost:3000 🌐

API🔗

>GET /students → list all students📋

>GET /students/:id → get a student👤

>POST /students → add a student➕

>PUT /students/:id → update a student✏️

>DELETE /students/:id → delete a student🗑️
